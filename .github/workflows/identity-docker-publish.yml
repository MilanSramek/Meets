name: IdentityDockerPublish

on:
  push:
    branches:
      - main
    paths:
      - "server/identity/**"
      - "server/common/**"
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/docker-publish.yml
    with:
      REGISTRY: ghcr.io
      REPOSITORY: ${{ github.repository }}
      IMAGE_NAME: identity